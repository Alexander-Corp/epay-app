package com.epay.app

import android.app.Application

class App : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialisation globale ici (ex: Dependency Injection)
    }
}
